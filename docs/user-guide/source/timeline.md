---
title: 时间线、缩放与时间码
description: 在源播放器中定位、滚动和缩放时间线，以及使用时间码进行精确跳转。
---

# 时间线、缩放与时间码

源播放器底部时间线用于浏览当前视频的帧范围。它显示当前游标、可见时间范围和刻度，并与播放位置同步。

## 定位与拖动

- 单击时间线位置可跳转到对应帧。
- 拖动游标可连续定位；播放器会在可见范围外时自动将时间线居中到当前帧。
- 使用上一帧/下一帧按钮或方向键做帧级微调。

## 平移和缩放时间线

- 使用时间线控制区的左右移动操作浏览更早或更晚的范围。
- 在时间线可视区域滚动可改变可见范围；按住 <kbd>Alt</kbd> 滚动时用于缩放时间线。
- 缩小可快速纵览长素材，放大可在剪辑点、字幕起止和镜头边界附近精细检查。

## 时间码编辑

时间码显示当前播放位置。单击或双击时间码可进入编辑状态：

1. 输入目标时间码或游标位置。
2. 按 <kbd>Enter</kbd> 确认跳转。
3. 按 <kbd>Escape</kbd> 取消修改。

输入内容无效或超出媒体范围时，LineCut 会限制在当前视频可用的帧范围内。

<GuideImage src="/images/source/timeline.png" alt="截图占位：放大的源播放器时间线" />

## 操作建议

- 确认字幕起止或镜头切点时，先放大时间线，再用方向键逐帧检查。
- 浏览长素材时，先缩小或平移，不要在极度放大的时间线中拖动很远距离。
- 如果时间线或预览长期无响应，先检查素材是否脱机、代理是否可用以及磁盘缓存空间。

## 与字幕和分镜的配合

时间线只控制当前活动视频的浏览位置。选中字幕或分镜后，先观察源监视器是否已定位到对应入点；如需精确确认范围，再放大时间线并逐帧检查。字幕、分镜和导出范围的改变不会通过拖动这条浏览时间线直接写入，避免把“定位”误当成“剪辑”。

当切换活动视频、重新链接素材或改用代理后，先重新定位一次再继续审看。这样可以确认时间码、缩略图与当前媒体来源仍一致。

参阅[预览、播放与逐帧控制](/source/preview-playback)和[性能、缓存与代理](/troubleshooting/performance)。
