---
title: 列表、图标与选择分镜
description: 浏览、搜索、筛选、排序和批量选择自动检测得到的分镜。
---

# 列表、图标与选择分镜

## 列表和图标视图

分镜面板底部可以切换两种浏览方式：

- **列表视图**：显示标题、媒体开始/结束、持续时间、关键字、星级、留用和标签等列。单击表头可排序，拖动列分隔可调整宽度，双击分隔线恢复默认宽度。
- **图标视图**：以缩略图卡片快速浏览画面。可从排序菜单选择标题、时间、时长、关键字、星级、留用或标签作为排序依据。

两种视图可分别调整缩略图/图标大小，切换不会删除标记、关键字或分镜。

## 搜索范围和规则

分镜搜索不只限于文字标题。可先选择范围，再选择规则：

| 范围             | 搜索对象                 |
| ---------------- | ------------------------ |
| 任何可搜索的字段 | 标题和关键字等可搜索文本 |
| 标题             | 仅分镜标题               |
| 关键字           | 关键字路径和同义词       |

| 规则            | 行为                     |
| --------------- | ------------------------ |
| 包含            | 文本中含有输入内容       |
| 包含所有        | 同时包含输入中的各个条件 |
| 包含单词        | 以词为单位匹配           |
| 不含            | 排除含有输入内容的分镜   |
| 开头为 / 结尾为 | 匹配开头或结尾           |
| 为空 / 不为空   | 查找未填写或已填写的字段 |

## 过滤器和快捷关键字过滤

与字幕面板相同，分镜可组合按旗标、已编辑/未编辑、星级比较和色标过滤。底部“仅展示选中分镜”可以临时只查看当前选择。

关键字树节点上的快捷过滤按钮会过滤拥有该关键字或其父级语义的分镜。多次启用快捷过滤时，结果需要满足所有启用的关键字条件。

## 选择方式

- 单击选择一个分镜，同时在源播放器跳转到该镜头。
- <kbd>Ctrl</kbd> + 单击添加或移除不连续选择。
- <kbd>Shift</kbd> + 单击选择从锚点到当前项的连续范围。
- 在空白区域拖拽框选可批量选择卡片或列表行；配合修饰键可以与原选择组合。
- **编辑 → 全选**只选择当前筛选后可见的分镜；**编辑 → 取消选择**清空当前选择。

<GuideImage src="/images/storyboard/views-selection.png" alt="截图占位：分镜列表的搜索、过滤与多选" />

选好分镜后，可以[标记分镜](/storyboard/annotations)、[建立关键字](/storyboard/keywords)或进入导出工作区。
