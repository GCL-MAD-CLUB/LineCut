---
title: 检测镜头切点
description: 用分镜面板为活动视频自动生成镜头，并理解重新切分和任务取消的影响。
---

# 检测镜头切点

分镜检测把当前活动视频按检测到的镜头切点拆成连续分镜，帮助你按画面而不是台词整理素材。检测结果保存到项目中。

## 开始检测

1. 在媒体箱中激活一个在线且可用的视频。
2. 打开**分镜**面板。
3. 点击面板标题行最右侧的剪刀按钮“切分”。
4. 在任务进度中等待完成，或在必要时取消。
5. 检测完成后，列表/图标区域显示按时间排列的分镜。

<GuideImage src="/images/storyboard/detection.png" alt="截图占位：分镜面板的切分按钮与检测进度" />

## 重新切分

已有分镜时，剪刀按钮显示“重新切分”。重新切分会用新的检测结果替换当前视频的分镜基础划分，因此先前围绕旧分镜建立的选择、标题、标记、堆叠和关键字可能不再适用。执行前建议保存项目副本。

## 检测条件与性能

- 必须先导入并激活一个可用视频；没有可用视频时按钮不可用。
- 检测使用本机模型，能够使用 DirectML 时优先采用 GPU，否则回退到 CPU。
- 视频越长、分辨率越高，任务耗时越长。代理用于预览，不替代需要分析的原始媒体。
- 任务取消后，不会把半完成结果作为新的完整检测结果写入项目。

## 检测后应做什么

1. 在图标视图快速浏览，检查误切或漏切是否影响后续整理。
2. 用星级、旗标和关键字标记可用镜头。
3. 将需要作为一个整体使用的连续镜头堆叠。
4. 选择最终镜头并导出。

## 将检测结果视为起点

检测产生的是按时间连续的镜头基础划分，并不判断镜头是否“可用”、是否属于同一叙事段落，也不替你命名。检测完成后先在图标视图快速扫一遍，再对明显的误切、漏切或需要作为一个整体的连续画面作人工整理。

如果准备重新切分，先判断你的目的：只是整理镜头组时应使用堆叠；只有基础切点本身不可靠时才重新切分。重新切分的影响范围更大，完成后还要重新检查标题、关键字、筛选和待导出选择。

继续阅读[列表、图标与选择分镜](/storyboard/views-selection)和[堆叠、展开和拆分分镜](/storyboard/stacks)。
