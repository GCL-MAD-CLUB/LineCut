P1：当前我们并没有利用起缓存路径的功能，我希望对每个媒体进行信息缓存，以大幅提升性能和速度。媒体的鉴别使用大量元数据组的hash。这需要涉及两台系统，分别放在缓存文件夹下的Media Cache和Media Cache Files文件夹。Media Cache内储存mcdb文件，用于在跨项目导入时，进行映射。Media Cache Files内储存lcbdc文件（全名时LineCut Media Cache File），是一种二进制文件，以项目内